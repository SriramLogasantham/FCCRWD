1. Html has oepning and closing tab<h1> and </h1>
    the Content goes between the tags.
2. H1 to h6 tags says the importance of contents, the lower the number the higher the importance.
    6 types of heading tag h1, h2, h3, h4, h5, h6
    rank based importance , we should use only one h1 element per page and lower important headings should be below h1
    ## SE0 --- Search engine optimization

 3. The p element is used to create a paragraph of text on websites.

 4. Commenting in html is used by the user to leave a message without affecting the browser display.
     It allows us to make the code inactive( the text given in comment doesnt work)

     A comment in HTML starts with <!--, contains any number of lines of text, and ends with -->.   
    
5. HTML5 has some elements that identify different content areas. 

   These elements make your HTML easier to read and help with Search Engine Optimization (SEO) and accessibilility.

6. Intendation-- We have to format the code which will make us to understand the code easily.
   Nesting -- we are giving a tag inbetween the other tags.

7.  img - we can add images in website by using img tag.
          **img elements have an opeing tag but doesnt have closing tag.

          Self closing tag is a tag which doesnt have closing tag.

8.           